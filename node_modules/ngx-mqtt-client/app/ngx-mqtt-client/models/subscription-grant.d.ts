import { ISubscriptionGrant, QoS } from 'mqtt';
export declare class SubscriptionGrant implements ISubscriptionGrant {
    topic: string;
    qos: QoS | number;
    constructor(grant: ISubscriptionGrant);
}
